# NoHallucinations Pipeline Configuration
# This file defines all training and evaluation settings

# Model settings
model:
  base_model: "HuggingFaceTB/SmolVLM-500M-Instruct"
  cache_dir: "../tmpcache"

# Training configurations to run
training:
  epochs: 3
  train_samples: 1000
  batch_size: 1
  gradient_accumulation_steps: 8
  learning_rate: 1e-5

  # Which training strategies to run
  strategies:
    - name: "baseline"
      enabled: true
      type: "none"  # No training, just evaluate base model

    - name: "sft_docvqa"
      enabled: true  # Now implemented
      type: "sft_benchmark"
      benchmark: "docvqa"

    - name: "sft_ocrbench"
      enabled: false  # Disabled: benchmark training not yet implemented
      type: "sft_benchmark"
      benchmark: "ocrbench"

    - name: "sft_chartqa"
      enabled: false  # Disabled: benchmark training not yet implemented
      type: "sft_benchmark"
      benchmark: "chartqa"

    - name: "erp_qcm_gemini"
      enabled: true
      type: "sft_qcm"
      dataset: "datasets/qcm/qcm_dataset_gemini.json"
      image_dir: "datasets/images"

    - name: "erp_qcm_nova"
      enabled: true
      type: "sft_qcm"
      dataset: "datasets/qcm/qcm_dataset_nova_pro.json"
      image_dir: "datasets/images"

    - name: "erp_dpo_gemini"
      enabled: false  # Disabled: DPO trainer needs image handling fix
      type: "dpo"
      dataset: "datasets/dpo/dpo_dataset_gemini.json"
      image_dir: "datasets/images"

    - name: "erp_dpo_nova"
      enabled: false  # Disabled: DPO trainer needs image handling fix
      type: "dpo"
      dataset: "datasets/dpo/dpo_dataset_nova_pro.json"
      image_dir: "datasets/images"

    - name: "erp_qcm_dpo_gemini"
      enabled: false  # Disabled: DPO trainer needs image handling fix
      type: "qcm_then_dpo"
      qcm_dataset: "datasets/qcm/qcm_dataset_gemini.json"
      dpo_dataset: "datasets/dpo/dpo_dataset_gemini.json"
      image_dir: "datasets/images"

    - name: "erp_dpo_qcm_gemini"
      enabled: false  # Disabled: DPO trainer needs image handling fix
      type: "dpo_then_qcm"
      dpo_dataset: "datasets/dpo/dpo_dataset_gemini.json"
      qcm_dataset: "datasets/qcm/qcm_dataset_gemini.json"
      image_dir: "datasets/images"

    - name: "erp_dpo_qcm_nova"
      enabled: false  # Disabled: DPO trainer needs image handling fix
      type: "dpo_then_qcm"
      dpo_dataset: "datasets/dpo/dpo_dataset_nova_pro.json"
      qcm_dataset: "datasets/qcm/qcm_dataset_nova_pro.json"
      image_dir: "datasets/images"

    # SFT on DPO datasets (using chosen responses only - no preference learning)
    - name: "erp_sft_dpo_gemini"
      enabled: true
      type: "sft_dpo"
      dataset: "datasets/dpo/dpo_dataset_gemini.json"
      image_dir: "datasets/images"

    - name: "erp_sft_dpo_nova"
      enabled: true
      type: "sft_dpo"
      dataset: "datasets/dpo/dpo_dataset_nova_pro.json"
      image_dir: "datasets/images"

    # Combined: QCM then SFT on DPO dataset
    - name: "erp_qcm_sft_dpo_gemini"
      enabled: true
      type: "sft_qcm_dpo"
      qcm_dataset: "datasets/qcm/qcm_dataset_gemini.json"
      dpo_dataset: "datasets/dpo/dpo_dataset_gemini.json"
      image_dir: "datasets/images"

    - name: "erp_qcm_sft_dpo_nova"
      enabled: true
      type: "sft_qcm_dpo"
      qcm_dataset: "datasets/qcm/qcm_dataset_nova_pro.json"
      dpo_dataset: "datasets/dpo/dpo_dataset_nova_pro.json"
      image_dir: "datasets/images"

# Evaluation settings
evaluation:
  benchmarks:
    - name: "ocrbench"
      enabled: true
      dataset: "echo840/OCRBench"
      split: "test"
      max_samples: 100  # Reduced for quick testing

    - name: "docvqa"
      enabled: true
      dataset: "nielsr/docvqa_1200_examples"  # Fixed: original dataset requires auth
      split: "train"
      max_samples: 100

    - name: "chartqa"
      enabled: true
      dataset: "HuggingFaceM4/ChartQA"
      split: "test"
      max_samples: 100

  # ERP-specific evaluations
  erp_evaluation:
    qcm:
      enabled: true
      dataset: "datasets/qcm/qcm_dataset_gemini.json"
      image_dir: "datasets/images"
      max_samples: 100  # Limit for testing

    qcm_claudette:
      enabled: true
      dataset: "datasets/qcm/qcm_claudette.json"
      max_samples: 10  # Limit for testing

    dpo_logprobs:
      enabled: true
      dataset: "datasets/dpo/dpo_dataset_gemini.json"
      image_dir: "datasets/images"
      max_samples: 10  # Limit for testing

    bertscore:
      enabled: false  # Disabled: too slow on CPU, use LogProb instead
      dataset: "datasets/dpo/dpo_dataset_gemini.json"
      image_dir: "datasets/images"
      max_samples: 10  # Limit for testing
      lang: "en"  # Language for BERTScore

# Paths
paths:
  output_dir: "modelweights"
  cache_dir: "datasets/cache"
  results_dir: "results"
  logs_dir: "logs"

# Pipeline options
pipeline:
  continue_on_error: true
  use_wandb: true
  wandb_project: "SmallVLM-NoHallucinations"
  debug_mode: false  # If true, use only 10 samples
  num_workers: 2
